/**
 * UI primitives — reusable design system components.
 *
 * Components:
 *  - Card      — surfaceCard with auto-shadow
 *  - Badge     — status pill / capsule
 *
 * All components consume useTheme() and design tokens automatically.
 * Design system: DS §8
 */
export { Card } from "./Card";
export { Badge } from "./Badge";
export type { BadgeStatus } from "./Badge";
